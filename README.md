# BITcore Skills Registry

Immutable, swarm-native skill registry.

## Structure
- `/sources`: Skill definitions by provider.
- `/schemas`: JSON schemas for validation.
- `/scripts`: CI and utility scripts.

## Usage
Agents fetch raw files, verify SHA256 hash against front-matter, and execute.
